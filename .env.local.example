# Development environment (generated by `npx convex dev`)
CONVEX_DEPLOYMENT=your-team:your-project-name

# Production environment (generated by `npx convex deploy`)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# Clerk Authentication
# Development keys (test_ prefix)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Production keys (live_ prefix) - replace with your production keys
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
# CLERK_SECRET_KEY=sk_live_...

# Additional environment variables for Convex functions
# Add any API keys or secrets here using:
# npx convex env set VARIABLE_NAME value
